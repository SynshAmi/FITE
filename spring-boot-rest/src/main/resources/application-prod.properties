# Production configuration profile

# Database
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Storage - use R2 in production
storage.type=r2
r2.bucket=${STORAGE_BUCKET}
r2.endpoint=${STORAGE_ENDPOINT}
r2.access-key=${STORAGE_ACCESS_KEY}
r2.secret-key=${STORAGE_SECRET_KEY}

# Chunk limits matching optimal benchmark
transfer.chunk-size-bytes=${MAX_CHUNK_SIZE:8388608}
spring.servlet.multipart.max-request-size=40MB
spring.servlet.multipart.max-file-size=50000MB
server.tomcat.connection-timeout=120000

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS}
